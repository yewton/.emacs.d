#+TITLE: ghostel
#+setupfile: ../setup.org

#+begin_src emacs-lisp :comments no :padline no
;;; toncs-config-ghostel.el -*- lexical-binding: t -*-
#+end_src

#+begin_src emacs-lisp
(require 'ghostel)
(require 'toncs-config-terminal)

(defun toncs-config-ghostel-configure ()
  (add-hook 'ghostel-mode-hook #'toncs-terminal-mode-setup))
#+end_src

#+begin_src emacs-lisp :comments no
(provide 'toncs-config-ghostel)
;;; toncs-config-ghostel.el ends here
#+end_src
