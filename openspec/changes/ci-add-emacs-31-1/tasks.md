## 1. ブランチ準備

- [x] 1.1 `change/ci-add-emacs-31-1` ブランチを作成する

## 2. CI マトリクスの更新

- [x] 2.1 `.github/workflows/ci.yml` の `emacs_version` マトリクスを `[29.2, 30.1, 30.2, snapshot]` から `[30.1, 30.2, 31.1, snapshot]` に変更する
- [x] 2.2 `README.org` の `Supports-Emacs_29.x` バッジを `Supports-Emacs_30.x` に更新する

## 3. 提出

- [x] 3.1 変更内容が意図通りか diff を確認する
- [ ] 3.2 ブランチを push し、Pull Request を作成する
